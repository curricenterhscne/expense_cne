# CLAUDE.md

이 저장소에서 작업할 때 반드시 먼저 읽으세요.

---

## 프로젝트 개요

충청남도교육청 중등교육과가 운영하는 **참학력 온라인 공동교육과정 수업운영비 신청서 수합 시스템**입니다.

### 업무 흐름

1. 교육청이 학기별 강좌 목록을 확정 → 스프레드시트 `강좌목록` 시트에 붙여넣음
2. 각 강좌 담당 교사가 웹 폼에서 신청서 작성·제출
3. 제출 즉시 신청서 PDF를 내려받아, **공문으로도 별도 제출** (증빙용)
4. 교육청은 공문 PDF로 증빙을 받고, 온라인 데이터로 집계·교부 처리

즉 **온라인 제출과 공문 제출이 짝을 이룹니다.** 온라인만 들어오고 공문이 안 온 건은 교부 대상이 아닙니다. 담당자가 `신청내역` 시트의 `접수상태` 열로 이를 관리하고, 집계표는 `접수` 상태인 건만 반영합니다.

### 규모와 주기

- **연 2회** (1학기 3월, 2학기 9월)
- 강좌 약 150개, 실제 신청 50~60건
- 동시 접속은 많아야 수 명. 성능 최적화보다 **정확성과 유지보수성**이 우선

### 운영 규칙

- 운영비는 **수업 교사(강사)의 소속교**로 교부
- 한도: **수강인원 × 20,000원** (단가는 `설정` 시트 값)
- 세부 항목 최대 **10개** (구분 / 산출식 / 금액)
- 문의: 담당 장학사 041-640-7221

---

## 기술 스택

| 계층 | 기술 |
|---|---|
| 프런트엔드 | 순수 HTML + CSS + Vanilla JS (프레임워크·빌드 없음) |
| 호스팅 | GitHub Pages (`docs/` 폴더) |
| 백엔드 | Google Apps Script 웹앱 |
| 데이터 | Google 스프레드시트 |
| GAS 배포 | clasp |
| PDF | 브라우저 인쇄 (`window.print()` + 인쇄용 CSS) |

**React·Vue·Tailwind·번들러를 도입하지 마세요.** 담당자가 직접 유지보수해야 하는 코드이고, 교육청 업무 PC 환경에서 빌드 도구 없이 파일만 열어 수정할 수 있어야 합니다. npm 의존성은 clasp 하나뿐입니다.

---

## 디렉터리 구조

```
expense_cne/
├─ CLAUDE.md                   ← 이 파일
├─ README.md
├─ docs/                       ← GitHub Pages 공개 대상
│   ├─ index.html              신청 폼 (단일 페이지)
│   ├─ css/
│   │   ├─ style.css           화면용
│   │   └─ print.css           인쇄(PDF)용
│   ├─ js/
│   │   ├─ config.js           GAS URL 등 상수
│   │   ├─ api.js              GAS 통신 (CORS 우회 포함)
│   │   ├─ auth.js             연락처 뒷 4자리 인증
│   │   ├─ form.js             폼 렌더·검증·제출
│   │   ├─ print.js            인쇄 뷰 생성·PDF 저장
│   │   └─ main.js             진입점·화면 전환
│   └─ manual/
│       ├─ teacher.html        교사용 매뉴얼 (A4 인쇄 가능)
│       └─ admin.html          담당자용 운영 매뉴얼
├─ gas/                        ← clasp 관리 (스프레드시트 바인딩)
│   ├─ .clasp.json             clasp 프로젝트 설정
│   ├─ appsscript.json         GAS 매니페스트
│   ├─ Constants.gs            시트명·열 인덱스 상수
│   ├─ Config.gs               설정 시트 읽기·캐싱 (5분)
│   ├─ Api.gs                  doGet / doPost 라우팅
│   ├─ Courses.gs              강좌 목록 조회 (연락처 제외)
│   ├─ Auth.gs                 뒷 4자리 검증·잠금·토큰
│   ├─ Submit.gs               신청 upsert (강좌코드 기준)
│   ├─ Summary.gs              집계표·미제출 목록 생성
│   ├─ Term.gs                 새 학기 시작 (보관→초기화)
│   └─ Menu.gs                 onOpen 커스텀 메뉴
├─ legacy/                     ← 기존 구현 (참고용, 수정 금지)
│   ├─ code.gs
│   └─ index.html
└─ .gitignore
```

---

## 스프레드시트 구조

시트명은 **정확히** 아래와 같습니다. 코드에서 상수로 관리하세요.

### `설정` — A열 키 / B열 값

`학기명`, `과정명`, `거점학교명`, `학기코드`, `인당단가`, `신청시작일`, `신청마감일`, `접수상태`, `담당자연락처`, `보관폴더ID`, `안내문구`

**학기명·단가·안내문구를 코드에 하드코딩하지 마세요.** 기존 구현의 가장 큰 유지보수 부담이 이것이었습니다. 반드시 `설정` 시트에서 읽습니다.

### `강좌목록` — 담당자가 매 학기 붙여넣기

```
과목코드 | 강좌코드 | 교육과정 | 과목명 | 전공 |
담당교사 | 교사소속교 | 교사연락처 | 수업요일 | 수강인원 | 비고
```

- `강좌코드`가 고유 키입니다 (예: `26-1가17_1`)
- 헤더 매칭은 **공백 제거 후 부분 일치**로 처리 (담당자가 `담당 교사`처럼 입력할 수 있음)
- `수강인원`에 `#N/A`가 섞여 들어오는 사례가 있었습니다. 숫자 변환 실패 시 0이 아니라 **명시적 오류**로 처리하고 해당 강좌를 선택 불가로 표시하세요

### `신청내역` — 코드가 자동 생성

```
접수상태 | 접수번호 | 최초제출일시 | 최종수정일시 | 수정횟수 |
강좌코드 | 과목코드 | 과목명 | 전공 | 담당교사 | 소속교(예산신청교) | 연락처 |
수업요일 | 수강인원 | 운영비희망여부 |
항목1_구분 | 항목1_산출식 | 항목1_금액 | ... | 항목10_금액 |
총소요예산 | 비고
```

- `접수상태`: `미접수` / `접수` / `보류` — 데이터 유효성 검사 드롭다운. **담당자가 수동 관리**
- 기존 구현은 이 열이 `1`, `2`, `공문보내겠다고 연락옴` 으로 뒤섞여 집계 누락이 발생했습니다. 반드시 드롭다운으로 강제하세요
- `접수번호`는 행 번호가 아니라 **독립 시퀀스**입니다 (`설정` 또는 `PropertiesService` 카운터). 기존 구현은 `getLastRow()`를 썼는데, 행 삭제 시 번호가 중복됩니다

### `집계표`, `미제출` — 코드가 자동 생성

집계표 레이아웃(병합셀, 색상, 항목별 행 분할)은 `legacy/code.gs`의 `buildSummarySheet()`를 참고하세요. **서식은 공문 첨부용으로 확정된 것이므로 임의로 바꾸지 마세요.**

---

## API 계약

GAS 웹앱 단일 엔드포인트. 요청은 모두 JSON.

### ⚠️ CORS 규칙 — 반드시 지킬 것

```js
// ✅ 올바름 — Content-Type 헤더를 생략해 preflight 회피
fetch(GAS_URL, { method: 'POST', body: JSON.stringify(payload) });

// ❌ 틀림 — preflight 발생 → GAS가 OPTIONS를 처리 못해 실패
fetch(GAS_URL, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(payload)
});
```

GAS 웹앱은 `OPTIONS` 요청에 응답할 수 없습니다. `Content-Type`을 지정하면 브라우저가 preflight를 보내고 요청이 실패합니다. 기존 구현이 이 방식으로 정상 동작 중이니 그대로 따르세요.

### GET

| action | 반환 | 비고 |
|---|---|---|
| `config` | 학기명, 과정명, 단가, 접수상태, 신청기간, 안내문구 | |
| `courses` | 강좌 목록 | **`교사연락처`를 절대 포함하지 말 것** |

### POST — body의 `action` 필드로 분기

| action | 입력 | 처리 |
|---|---|---|
| `verify` | `courseCode`, `last4` | 서버에서 연락처 뒤 4자리 대조. 성공 시 1회용 토큰 + 기존 신청 내역 반환 |
| `submit` | `token`, 신청 데이터 | 토큰 검증 후 upsert |

### 🔒 개인정보 규칙

- **강좌 목록 응답에 교사 연락처를 넣지 마세요.** 기존 구현은 `?action=courses` 응답에 교사 155명의 휴대폰 번호를 전부 포함하고 있었습니다. 공개 URL이므로 누구나 조회 가능한 상태였습니다
- **뒤 4자리를 URL 쿼리스트링에 넣지 마세요.** 인증은 반드시 POST body로
- 대조는 **서버(GAS)에서만** 수행합니다. 클라이언트로 연락처를 내려보내 비교하면 인증이 무의미합니다

---

## 핵심 설계 결정

### 1. 제출은 append가 아니라 upsert

`신청내역`에서 `강좌코드`로 기존 행을 찾아, 있으면 **덮어쓰기**, 없으면 추가합니다.

- `접수번호`, `최초제출일시`는 유지
- `최종수정일시` 갱신, `수정횟수` +1
- `접수상태`는 **덮어쓰지 않음** (담당자가 설정한 값 보존)

26-1에서 54개 강좌에 58건이 들어와 담당자가 중복을 수동으로 골라냈습니다. 이 문제를 구조적으로 제거하는 것이 이번 재개발의 핵심 목표 중 하나입니다.

### 2. PDF는 인쇄 방식

기존 구현은 jsPDF에 한글 폰트가 없어 canvas에 좌표를 하나씩 찍었습니다. 서식을 조금만 바꿔도 좌표를 다시 계산해야 했습니다.

새 방식: 화면에 숨겨진 A4 레이아웃 `<section id="printSheet">`을 만들고, `print.css`의 `@media print`에서 그 영역만 노출한 뒤 `window.print()`를 호출합니다.

- 파일명은 `document.title`을 따라갑니다. 인쇄 직전에 `26-2_수업운영비신청서_26-2가17_1_최서경` 형태로 설정하고, 인쇄 후 원복하세요
- jsPDF 의존성은 **완전히 제거**합니다

### 3. 스프레드시트는 매 학기 교체하지 않음

파일을 복사하면 스크립트 ID가 바뀌어 웹앱 URL이 바뀌고, 교사 안내 링크와 `config.js`를 매번 고쳐야 합니다.

대신 `새 학기 시작` 메뉴가 **사본을 보관용으로 떼어내고 원본을 비웁니다.** 순서를 지키세요.

1. `DriveApp.makeCopy()`로 `[보관] {학기코드} 수업운영비` 생성 → `보관폴더ID`로 이동
2. **복사 성공을 확인한 뒤에만** 다음 단계 진행
3. `신청내역` 2행 이하 삭제, `집계표`·`미제출` 시트 삭제
4. `강좌목록` 2행 이하 삭제
5. `설정`의 `학기명`·`학기코드`·`접수상태` 갱신 안내

확인 대화상자에 삭제될 건수를 표시하세요. 복사에 실패하면 **반드시 중단**합니다.

---

## 코딩 규칙

- **주석·UI 문자열은 한국어.** 담당 장학사가 직접 코드를 읽고 수정합니다
- 변수·함수명은 영어 camelCase
- 파일 하나가 300줄을 넘으면 분리를 제안하세요. 기존 `index.html`이 990줄 단일 파일이라 수정할 때마다 전체를 읽어야 했습니다
- 외부 CDN 의존을 추가하지 마세요 (교육청 망에서 차단될 수 있음). 기존 jsPDF CDN도 제거 대상입니다
- GAS에서 시트 접근은 루프 안에서 `getRange()`를 반복 호출하지 말고, `getValues()`로 한 번에 읽어 메모리에서 처리하세요
- 동시 제출 대비: 쓰기 작업은 `LockService`로 감쌉니다 (기존 구현 유지)

### 하지 말 것

- `legacy/` 폴더 수정
- 집계표 서식 임의 변경
- 학기명·단가 하드코딩
- 강좌 목록 응답에 연락처 포함
- 프레임워크·빌드 도구 도입
- `신청내역` 시트의 열 순서 변경 (집계표 로직이 인덱스에 의존)

---

## 개발·배포 명령

```bash
# 최초 1회
npm i -g @google/clasp
clasp login

# 기존 프로젝트 연결 (gas/ 폴더에서)
cd gas && clasp clone <스크립트ID>

# 코드 반영
clasp push

# 브라우저에서 열기
clasp open
```

**웹앱 URL을 유지하려면** 배포 시 `새 배포`가 아니라 **기존 배포 편집 → 버전: 새 버전**을 선택하세요. `새 배포`를 하면 `/exec` URL이 바뀌어 교사 안내 링크가 전부 무효화됩니다.

프런트엔드는 `main` 브랜치에 push하면 GitHub Pages가 자동 반영합니다. (Settings → Pages → Source: `main` / `docs`)

---

## 테스트 방법

빌드가 없으므로 `docs/index.html`을 브라우저로 직접 열어 확인합니다. 단, `file://`에서는 GAS 요청이 CORS로 막힐 수 있으니 간이 서버를 씁니다.

```bash
cd docs && python3 -m http.server 8000
```

테스트 시나리오는 최소 이만큼 확인하세요.

1. 강좌 선택 → 뒷 4자리 정답 → 폼 진입
2. 뒷 4자리 오답 5회 → 잠금
3. 한도 초과 금액 입력 → 제출 차단
4. 희망하지 않음 선택 → 항목 없이 제출
5. **동일 강좌 재제출 → 행이 늘지 않고 덮어써지는지** (가장 중요)
6. 재제출 후 `접수상태` 값이 보존되는지
7. `설정`의 `접수상태`를 `마감`으로 변경 → 폼 잠김
8. 인쇄 미리보기에서 A4 1장에 들어가는지

---

## 구현 현황 (2026-08-03 기준)

모든 Phase 구현 완료. 배포·테스트 단계.

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 저장소 골격 생성 | ✅ 완료 |
| 1 | 설정 계층 + GAS 라우팅 (Constants, Config, Api, Menu) | ✅ 완료 |
| 2 | 강좌 조회 + 신청 제출 upsert (Courses, Submit) | ✅ 완료 |
| 3 | 교사 본인 확인 — 뒤 4자리 + 1회용 토큰 (Auth) | ✅ 완료 |
| 4 | 프런트엔드 이식 — 990줄 단일 파일 → 7개 모듈 분리 | ✅ 완료 |
| 5 | 인쇄(PDF) — window.print() + print.css | ✅ 완료 |
| 6 | 담당자 관리 — 집계표, 미제출 목록, 새 학기 시작 | ✅ 완료 |
| 7 | 매뉴얼 (교사용·담당자용 HTML) + README 갱신 | ✅ 완료 |

### 배포 정보

- **GitHub Pages**: https://curricenterhscne.github.io/expense_cne/
- **교사용 매뉴얼**: https://curricenterhscne.github.io/expense_cne/manual/teacher.html
- **담당자용 매뉴얼**: https://curricenterhscne.github.io/expense_cne/manual/admin.html
- **GAS 웹앱 URL**: `docs/js/config.js`의 `GAS_URL` 참조
- **GAS 스크립트 ID**: `gas/.clasp.json`의 `scriptId` 참조

### 알려진 이슈·주의사항

- `clasp push` 후 반드시 **기존 배포 편집 → 새 버전**으로 배포해야 코드가 반영됨
- `새 배포`를 하면 URL이 바뀌므로 절대 사용 금지
- `print.css`는 `media="print"`로 로드되므로, 인쇄 영역 숨김(`.print-sheet { display: none }`)은 `style.css`에 있어야 함
- GAS 설정 캐시는 5분 — 설정 변경 후 최대 5분 대기 필요
